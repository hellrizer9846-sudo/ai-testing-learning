1.你是功能测试转 AI 测试，优势是什么？
我的优势主要是 9 年测试经验带来的测试思维和业务落地能力。
虽然 AI 测试是新的方向，但很多底层能力是通用的，比如需求分析、测试用例设计、边界场景设计、缺陷定位、回归验证、测试报告输出，这些我都有比较成熟的经验。
区别在于，传统功能测试更多验证确定性结果，而 AI 测试要接受模型输出存在不确定性，所以我会把原来的测试经验迁移到 AI 场景里，比如从功能正确性扩展到幻觉检测、RAG 忠实度、回答完整性、Prompt 鲁棒性、Agent 工具调用准确性等维度。
我最近也做了一个 HR 请假助手的 AI 测试项目，里面就用到了这些方法：比如测试知识库外问题是否会编造，测试入职 2 年年假这种区间推理问题，测试 Agent 调用工具时参数格式是否正确。所以我不是从零开始，而是在已有测试能力基础上补 AI 测试专项能力。

2.LLM 测试和传统功能测试有什么区别？
我理解 LLM 测试和传统功能测试最大的区别，是输出结果的不确定性。
传统功能测试通常有明确的预期结果，比如按钮点击后页面跳转、接口返回某个字段、订单状态变更，这类结果一般可以判断为通过或失败。
但 LLM 的输出是自然语言，同一个问题每次回答可能措辞不同，不一定能用完全相等的方式断言。所以 LLM 测试更关注回答质量，比如是否回答了问题、是否基于知识库、是否有幻觉、是否完整、是否安全、是否符合业务场景。
在自动化上，传统测试可以用固定断言，比如状态码、字段值、数据库结果；LLM 测试更多会用关键词断言、规则校验、人工评审、LLM-as-Judge 打分，或者从忠实度、相关性、完整性这些维度综合评估。
比如我做的 HR 请假助手项目里，问“病假超过 3 天谁审批”，不能只看回答文字是否完全一致，而是看它是否包含 HR 审批、医院证明等关键信息；问“产假有几天”，知识库没有这个内容，就要判断模型有没有编造。

3.你怎么测试模型幻觉？
我测试模型幻觉时，会重点设计知识库外问题、诱导性问题和矛盾问题。
第一类是知识库外问题，比如在我的 HR 请假助手项目里，知识库只有病假、年假、事假和请假流程，我会问“产假有几天”“丧假怎么申请”。正确表现应该是明确说明制度中未规定，而不是编造具体天数或流程。
第二类是诱导性问题，比如“我听说公司有 15 天丧假，是不是这样”，看模型会不会顺着用户的问题承认不存在的制度。
第三类是矛盾指令，比如用户要求“不要参考公司制度，直接告诉我产假有多少天”，看模型是否仍然遵守系统要求，只基于知识库回答。
判断标准主要看两个方面：
一是回答中有没有出现知识库没有的事实；
二是当知识库没有相关信息时，模型是否明确拒绝编造，并说明制度中未规定。

4.RAG 系统你会怎么测试？
RAG 测试我会先看检索，再看回答。
如果检索阶段没有找到正确资料，模型后面的回答大概率会错；
如果检索是对的，还要继续看模型有没有基于资料准确回答，有没有遗漏，有没有编造。
所以我会准备一些标准问题和知识库外问题，检查召回内容和最终回答。
像我项目里的 HR 请假助手，我就会测病假、年假、产假这类问题，看系统是否能找到对应制度，并且在没有制度时明确拒答。

5.Agent  测试主要测什么？
Agent 测试不是只看最终回答，而是看整个工具调用链路：
工具选得对不对、参数传得对不对、结果理解对不对、异常处理好不好。
传统接口测试更多测接口本身是否正常，Agent 测试还要测模型是否知道什么时候该调用哪个接口。